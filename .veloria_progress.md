# TASK PROGRESS

## OBJECTIF ORIGINAL
Site de faux casino (fausse monnaie) multijoueur : roulette, blackjack, poker.
Comptes réels + multijoueur, UI/UX « dopamine » inspirée des gros sites. Classement
des joueurs les plus riches + « argent gagné par le casino ». Prêt banque (7j puis
intérêts), prêts/dons entre joueurs (intérêts négociés à 2), parrainage à code
irréversible. Age gate 18+, RGPD, message d'avertissement fausse monnaie. Roulette
en mode plein écran « Table Réelle ». Blackjack : tables créables, auto-supprimées si
vides. Ajouts : connexion Google via Supabase + faire fonctionner le site déployé.

## STATUT
- [x] Étape 1 : Backend NestJS complet (auth, économie, prêts, parrainage, leaderboard, jeux) + 108 tests
- [x] Étape 2 : Frontend React/Vite complet (pages, design dopamine, légal/RGPD)
- [x] Étape 3 : Prisma schema + migrations + seed
- [x] Étape 4 : Déploiement frontend Vercel (CI verte)
- [x] Étape 5 : Connexion Google via Supabase (code, mergé main 2f71dbe)
- [ ] Étape 6 : Activer le provider Google dans Supabase (dashboard) ← UTILISATEUR
- [ ] Étape 7 : Héberger le backend NestJS (corrige « Network Error » prod) ← EN COURS
- [ ] Étape 8 : VITE_API_URL (Vercel) → URL backend + DATABASE_URL → Postgres Supabase

## DERNIÈRE ACTION COMPLÉTÉE
Fichier : auth.service.ts / AuthCallback.tsx / GoogleButton.tsx (PR #3)
Action : Connexion Google via Supabase, mergée sur main, prod Vercel verte (build)
Constat : « Network Error » après retour Google car le frontend prod appelle le backend
sur http://localhost:4000 (backend non hébergé).

## PROCHAINE ACTION (si coupure)
Fichier cible : infra (hébergement backend) + frontend/.env (VITE_API_URL)
Action à faire : héberger le backend NestJS (host Node persistant, WS) ; DATABASE_URL →
Postgres Supabase ; régler VITE_API_URL sur Vercel ; activer Google dans Supabase Auth.
Ligne de reprise : voir CLAUDE.md → PROCHAINE ACTION IMMÉDIATE.

## ERREURS / BLOCAGES EN COURS
- BUG RENDER (P1001) — RÉSOLU CÔTÉ CODE : auto-bascule URL directe (IPv6) → Session pooler
  (IPv4) au démarrage. `scripts/db-deploy.cjs` teste aws-0/aws-1 (eu-north-1) avec le vrai
  mot de passe, choisit le shard joignable, migre, persiste l'URL ; `main.ts` la réutilise.
  Helper: `src/common/supabase-db-url.ts`. Aucun changement Render requis ; redéploie `main`.
  Fallback si les 2 shards échouent : message clair + exit 1 (mettre SUPABASE_POOLER_HOST).
- Provider Google non activé côté Supabase → 400 « provider is not enabled » (action utilisateur).
- Schéma DB : créé automatiquement par `migrate deploy` au 1er démarrage Render réussi.

## FICHIERS CRÉÉS / MODIFIÉS
- backend/** ✅ (NestJS complet)
- backend/prisma/schema.prisma + migrations (0000_init, 0001_oauth_google) + seed.ts ✅
- frontend/** ✅ (toutes pages + Google: supabase.ts, GoogleButton.tsx, AuthCallback.tsx) ✅
- docs/ (API_CONTRACT, WEBSOCKET_EVENTS, SUPABASE_GOOGLE_AUTH) ✅
- vercel.json ✅, README.md ✅, CLAUDE.md ✅, .veloria_progress.md ✅

## FICHIERS RESTANTS À CRÉER
- Entrée/config de déploiement backend (selon l'hôte choisi) ← EN COURS
